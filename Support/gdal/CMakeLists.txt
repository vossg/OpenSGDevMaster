CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

CMAKE_POLICY(SET CMP0011 OLD)

IF(OSGGdalSrcDir)

IF(OSGGdalSrcDir MATCHES "1.5.4$")
  INCLUDE("CMakeLists_154.txt")
ELSE()
  INCLUDE("CMakeLists_current.txt")
ENDIF()

ELSE(OSGGdalSrcDir)
  SET(OSGGdalSrcDir "" CACHE PATH "")
ENDIF(OSGGdalSrcDir)
