#!python
#

# For now ignore this code because it causes a cycle in the
# library dependencies.
# RenderTraversal depends upon Window and Window depends upon RenderTraversal
stop_traversal = True

if option_pass:
   print "Processing option pass"
else:
   library = "OSGRenderTraversal"
   osg_dep_libs = ['OSGBase','OSGSystem','OSGWindow']

   # Tests' dependencies

   osg_test_libs = [ 'OSGGroup', 'OSGDrawable' ]
   test_cpppath = []
   test_libpath = []
