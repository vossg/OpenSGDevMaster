
CHECK_PROJECT("OSGSystem")

IF(NOT OSG_ENABLE_NEW_SHADER)
    RETURN()
ENDIF(NOT OSG_ENABLE_NEW_SHADER)

IF(OSG_VERBOSE_SCAN)
    MESSAGE( STATUS "Looking at : ${CMAKE_CURRENT_SOURCE_DIR}")
ENDIF(OSG_VERBOSE_SCAN)

COLLECT_SUBDIRS()
