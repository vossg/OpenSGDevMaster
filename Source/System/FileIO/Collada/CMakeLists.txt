
CHECK_PROJECT("OSGFileIO")

IF(NOT COLLADA_FOUND)
    RETURN()
ENDIF(NOT COLLADA_FOUND)

IF(OSG_VERBOSE_SCAN)
    MESSAGE( STATUS "Looking at : ${CMAKE_CURRENT_SOURCE_DIR}")
ENDIF(OSG_VERBOSE_SCAN)

PROCESS_DIR()
