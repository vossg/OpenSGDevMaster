
#!python
#
if option_pass:
   print "Processing option pass"
else:
   
   # This is part of the System lib, but not the main file for it.
   # See Source/System/build.info for that.
   library = "OSGSystem"

   scanparse_opt = opts.GetOption("enable_scanparse_in_builddir")

   if scanparse_opt.getValue() == True:
      stop_traversal = True
