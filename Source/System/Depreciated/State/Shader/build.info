#!/usr/bin/python

if option_pass:
   print "Processing option pass"
else:
    library = "OSGSystem"
    
    enable_newshader = opts.GetOption("enable_new_shader")

    if enable_newshader.getSettings()[0][1] == False:
        stop_traversal = True
    else:
        stop_traversal = False
   
    if stop_traversal == False:

        # ignore this directory if "disable_deprecated" option is true.
        disable_deprecated = opts.GetOption("disable_deprecated")

        if disable_deprecated.getSettings()[0][1] == False:
            stop_traversal = False
        else:
            stop_traversal = True

