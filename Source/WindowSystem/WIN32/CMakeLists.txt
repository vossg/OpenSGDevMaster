
IF(NOT OSGBUILD_WINDOWWIN32)
    RETURN()
ENDIF(NOT OSGBUILD_WINDOWWIN32)

IF(NOT WIN32)
    RETURN()
ENDIF(NOT WIN32)

IF(OSG_VERBOSE_SCAN)
    MESSAGE( STATUS "Looking at : ${CMAKE_CURRENT_SOURCE_DIR}")
ENDIF(OSG_VERBOSE_SCAN)

CLEAR_PROJECT(OSGWindowWIN32)

SET(OSGWindowWIN32_OSG_Dependencies OSGBase OSGSystem)

SETUP_PROJECT(OSGWindowWIN32 OSG_COMPILEWINDOWWIN32LIB)
