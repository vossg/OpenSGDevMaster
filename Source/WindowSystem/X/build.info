#!python
#
if platform in ("win32", "darwin"):
    stop_traversal = True      
else:
    if option_pass:
        print "Processing option pass"
    else:
        x_option = opts.GetOption("X")
        if not x_option.isAvailable():
            stop_traversal = True
        else:
            libs.append(x_option.library)

            library = "OSGWindowX"
            osg_dep_libs = ['OSGBase', 'OSGSystem','OSGWindow', 'OSGDrawable',
                            'OSGUtil']
