
IF(NOT GLUT_FOUND OR NOT OSGBUILD_WINDOWGLUT)
    RETURN()
ENDIF(NOT GLUT_FOUND OR NOT OSGBUILD_WINDOWGLUT)

IF(OSG_VERBOSE_SCAN)
    MESSAGE( STATUS "Looking at : ${CMAKE_CURRENT_SOURCE_DIR}")
ENDIF(OSG_VERBOSE_SCAN)

PROCESS_DIR()
