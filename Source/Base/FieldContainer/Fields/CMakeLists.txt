
IF(OSG_VERBOSE_SCAN)
    MESSAGE( STATUS "Looking at : ${CMAKE_CURRENT_SOURCE_DIR}")
ENDIF(OSG_VERBOSE_SCAN)

PROCESS_DIR()

ADD_SUBDIRECTORY(PointerFields)
ADD_SUBDIRECTORY(Handle)
ADD_SUBDIRECTORY(Wrapper)

